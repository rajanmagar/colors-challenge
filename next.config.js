/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  basePath: "/colors-challenge",
  assetPrefix: "/colors-challenge"
}

module.exports = nextConfig
